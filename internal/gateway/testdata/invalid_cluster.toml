# A [[node]] pinned to a host id that no [[host]] defines must be rejected.
version = "1.0"
region = "ap-southeast-2"

[[host]]
id = 1
bind_addr = "0.0.0.0:6660"
public_addr = "10.11.12.1:6660"
data_dir = "/var/lib/predastore"

[[node]]
id = 1
host_id = 2
role = "shard-storage"
