# System user for predastore.service. The home directory is the data root
# rather than a real home, which ProtectHome=yes hides anyway.
u predastore - "Predastore object storage" /var/lib/predastore /usr/sbin/nologin
